{
  "configurations": [
    {
      "name": "C/C++: clang build and debug active file",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": false, // Set to true if you want to start at main()
      "cwd": "${fileDirname}",
      "environment": [],
      "externalConsole": true, // Change to true if you want a separate console
      "MIMode": "lldb",
      "preLaunchTask": "C/C++: clang build active file"
    }
  ],
  "version": "2.0.0"
}
